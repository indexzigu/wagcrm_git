# WAG CRM Workspace Customization Rules

- **Security & Backup Operations:** Whenever handling issues, user requests, or alerts related to database backups, environment variables, credentials leakage, or state restoration, you MUST strictly adhere to the guidelines documented in `/Users/z9/.gemini/antigravity/scratch/wag-crm/SECURITY_AND_BACKUP_MANUAL.md`. Do not make assumptions about backup artifacts or credential rotation without consulting this manual first.

- **Skill Lookup Enforcement for Tech Proposals:** Before proposing any architectural changes, features, or external integrations (e.g., LLM APIs, Database configurations, Cloud Services), you MUST check if a corresponding skill exists in the `<skills>` block. If a matching skill exists, you MUST use `view_file` to read its full `SKILL.md` before writing the implementation plan. Do not rely solely on internal pre-trained weights, as doing so leads to out-of-date or violating recommendations (e.g., suggesting deprecated model versions or legacy design patterns).
- **Conservative Automation Philosophy:** Never propose automated background tasks (cron jobs, polling) for external API calls (e.g., LLM generation per post) simply for convenience. Background API calls must be treated as a severe cost risk. Always default to "Active, user-initiated (on-demand)" triggers unless the business scale strictly necessitates and explicitly approves a batch background process.
