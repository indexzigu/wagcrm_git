# WAG CRM Project Governance Rules

이 프로젝트 지침은 전역 지침과 동일하게 실패 위험도 순서로 정렬한다.
상위 우선순위 규칙은 구현, 디버깅, 검증, 보고 시 먼저 확인해야 한다.

**구조:** 이 파일은 항상 로드되는 **강제 코어**(P0·P1·P4·필독 라우터·문서
정책·완료의 정의)다. 도메인 규칙 P2·P3·P5(제품/UX), P6(배포·DB),
P7(데이터 계약), P8(디자인), P9(개발환경/QA)는 `docs/agents/` 모듈로
분리되어 있고, 아래 **Mandatory Reading Router**가 읽기를 강제한다.
P0~P9 번호는 기존 문서·메모리·대화가 참조하는 안정 식별자이므로 재부여
금지. 사용자 소통·보고·PR 본문은 한국어가 기본이다(코드·식별자·기술 용어는
영문 유지).

## Project Identity & Session Protocol

- **무엇을 위한 시스템인가:** 인플루언서 공동구매(공구) 캠페인 운영 CRM.
  셀러 발굴 → 아웃리치 → 캠페인(딜 × 셀러 × 회차) → 발주 → 네이버 주문
  동기화 → 정산 → 성과 리포트/셀러 포털까지, 운영자 한 사람이 전 과정을
  돌리는 내부 도구다.
- **사용자:** 내부 운영자(오너) 1인 + 읽기 전용 셀러(포털 `/<slug>`,
  `/p/[token]`). 다중 테넌트 SaaS가 아니다 — 모든 화면·기능 판단 기준은
  "오너의 운영 의사결정 지원"이다(P2).
- **저장소:** `indexzigu/wagcrm` — 2026-07-16 구 레포(`indexzigu/wag-crm`)에서
  이관됐다(두 레포 이력은 서로 무관하다). **2026-08-26 부터 PRIVATE 이다**
  (API `visibility: private` 실측, 포크 0건). ⚠️ **그러나 2026-07-21 ~ 08-26
  약 5주간 PUBLIC 이었다** — 그 기간의 커밋·PR·브랜치명·이력은 이미 밖으로
  나갔고 **비공개 전환으로 회수되지 않는다**. 그래서 P0 의 데이터 노출 규칙은
  완화 대상이 **아니다**(사유는 P0 Public Repo Data Guard).
- **프로덕션:** `crm.ygrd.kr` (region `hnd1`). **머지 ≠ 배포다** — `main`은
  Vercel 빌드를 만들지 않는 **통합 전용** 브랜치이고, 프로덕션 배포는
  `release` 브랜치 **승격** 시점에 일어난다(실행체
  `scripts/promote-prod.sh`, 자동화 `.github/workflows/promote-auto.yml` —
  미승격 ≥5건이면 즉시, 아니면 4시간 스케줄, 커밋 메시지
  `[promote]`/`[deploy]`로 문턱 무시 즉시 승격). 승격 정책·플랜 한도·배포
  확인 절차의 **정본은 `docs/agents/deployment.md`(P6)**다 — 배포에 얽힌
  판단 전에 그 모듈 전문을 읽는다.
  ⛔ 종전 서술 "origin/main 머지 = 곧 프로덕션 배포"는 **SUPERSEDED**
  (2026-07-24 승격 레인 전환 — 빌드 비용 배칭). 머지를 배포로 오인해
  "내 커밋이 왜 prod에 없나"를 버그로 조사하지 않는다.
- **세션 프로토콜:** 시작 시 **메인 레포 루트**의 `PROJECT_MASTER.md`(작업
  보드/Handoff)를 읽고, 종료 전 Handoff 섹션을 갱신한다. ⚠️ 이 파일은 git
  **미추적 로컬 전용**이라(아래 문서 관리 정책 모드 L) fresh clone·클라우드
  세션·신규 워크트리에는 **존재하지 않는다** — 없으면 없는 대로 진행하고,
  그 세션의 인계 사항은 **메인 레포의 `docs/handoff/` 파일**에 남긴다(워크트리
  세션도 메인 레포 사본에 쓴다 — 아래 모드 L). PR 본문에 남기는 것은 로컬
  파일시스템에 접근할 수 없는 환경(클라우드 세션)의 **최후 예외**이며, 그때도
  P0 「PR 본문 최소화」의 금지 범주를 지킨다. 다음 에이전트는 이 세션을 전혀
  기억하지 못하는 다른 도구(Claude/Codex/Antigravity)일 수 있다. 세션에서
  첫 코드 수정 전에 `docs/agents/codebase-map.md`(코드·문서 지도)를 1회
  읽는다.
- **작업 병합(Batching) 원칙:** 원칙 정본은 전역
  `~/.gemini/config/rules/rules-planning.md` §Work Batching이다(연관 소작업은 한
  워크트리·PR에 누적, 중간 저장 제안은 맥락 단절·고복잡 신규 작업 시에만 —
  2026-08-01 승격). 이 레포의 비용 모델(절약 대상은 승격=빌드 횟수이지 머지
  횟수가 아니다)과 배칭이 여전히 유용한 국면은 P6 `docs/agents/deployment.md`
  「작업 배칭」이 정본이다.

## P0. Critical Safety and Explicit Deny Rules

- **Destructive System Actions:** 루트 디렉터리 또는 시스템 설정을 대상으로
  하는 명령은 사용자 확인 없이 실행하지 않는다. 예: `rm -rf /`, 시스템 파일
  대상 `chmod`.

- **Critical Data Loss:** 보호 브랜치(`main`, `master`)에 대한 강제 푸시,
  원격 저장소 삭제, 대량 데이터 삭제처럼 복구가 어려운 작업은 명시적 확인
  없이는 수행하지 않는다.

- **Sensitive Credential Exposure:** `.env` 또는 secret 파일의 내용을 외부
  로그/API에 출력, 업로드, 이동하지 않는다. 필요한 경우 존재 여부만 확인하고
  값은 노출하지 않는다.

- **Financial or Account Changes:** 결제 설정 변경, 유료 서비스 구독, 계정
  권한 변경은 사용자 확인 없이 수행하지 않는다.

- **CRM/UX Intent Before Execution:** 사용자 피드백을 "삭제", "추가", "크기
  조정" 같은 단순 작업으로 축소하지 않는다. 먼저 운영 CRM 흐름, 의사결정
  목적, 정보 계층에 어떤 의미가 있는지 해석한다.

- **Repo .env Is Production DB:** 레포 `.env`의 `DATABASE_URL`은 **실제
  프로덕션 DB**다 — 2026-08-13 셀프호스트 컷오버 이후로는 **이 맥에서 도는
  셀프호스트 Supabase 스택**(`127.0.0.1:55432` = `supabase-db` 컨테이너)을
  가리킨다(2026-08-26 실측). ⛔ 종전 서술 「클라우드 Supabase 프로젝트」는
  **SUPERSEDED** — 지금 클라우드 Supabase 는 **은퇴 DB**이고, 그것을 프로덕션으로
  착각하면 진단이 정반대로 간다(실사고:
  `docs/archive/handoff/kakao-ingest-writes-to-retired-db.md` — 수집 잡이 구 배포
  URL 로 은퇴 DB 에만 쓰고 있었다).
  ⚠️ **호스트가 로컬이 됐다고 위험이 내려간 것이 아니다** — 같은 머신이라 오히려
  더 가깝다. `npm run dev`와 `scripts/*` 실행은 여전히 곧 프로덕션
  데이터 작업이다. ⛔ `127.0.0.1` 을 보고 "로컬이니 안전"으로 읽지 말 것 —
  판정 SSOT `isRemoteDatabaseUrl()`(`src/lib/prisma-client.ts`)도 호스트가
  아니라 **sqlite 여부**로 가른다(로컬 postgres = remote 취급, 2026-08-26 실측).
  대량 수정·삭제 스크립트는 오너 확인 없이 실행하지 않고,
  QA용 테스트 데이터는 주입 후 즉시 삭제하며, 실제 발주 메일·발송지연 처리
  등 외부 부수효과가 있는 버튼·API는 QA 중 절대 호출하지 않는다. Vercel의
  sensitive env는 `vercel env pull` 시 빈값으로 내려온다 — "값이 없다"고
  오판하지 말 것.

- **Public Repo Data Guard:** 일반 원칙 정본은 전역
  `~/.gemini/config/rules/security.md` §Public Repository Exposure다(공개 레포에선
  커밋·PR·브랜치명·주석·추적 문서가 push 즉시 공개 — 2026-08-01 승격). 아래는 이
  레포의 실사고·강제 장치다. ⚠️ **2026-08-26 비공개 전환 후에도 이 규칙을 그대로
  유지한다** — ①공개 기간(2026-07-21~08-26)의 이력은 **이미 나갔고 되돌릴 수 없다**
  ②가시성은 되돌릴 수 있는 **설정**이라 다시 공개되면 그 시점의 전체 이력이 한꺼번에
  나간다 ③습관이 무너진 뒤에는 다음 공개 때 복구할 방법이 없다. 즉 지금 이 규칙은
  「현재 진행형」이 아니라 **되돌릴 수 없는 과거 + 되돌릴 수 있는 미래**를 상대한다.
  커밋 메시지·PR 본문·**브랜치명**·코드
  주석·레포 내 문서에 셀러 실명, 프로덕션 실측 지표(매출·수량 등), 시크릿
  값을 넣지 않는다(push 분류기가 차단한 실사고 있음). ⚠️ **브랜치명이 오래
  빠져 있던 사각이다** — 하네스가 **세션 제목에서 브랜치명을 자동 파생**하므로
  제목에 실명이 들어가면 그대로 브랜치가 된다. 2026-07-31 실제로 셀러 실명을
  로마자로 담은 브랜치가 생성됐고 **push 직전에 사람 눈으로 발견해 개명**했다
  (원격 유출 0 확인). 브랜치명은 push 순간 공개되고 PR 페이지·`git ls-remote`
  에 남는다 — **push 전 브랜치명 1회 확인**(P6 커밋·푸시 절차). ⚠️ **시크릿은 특히
  주의** — 2026-07-21 공개 전환 시 이력 재작성은 PII만 대상으로 했고
  **시크릿은 스크럽되지 않아 그대로 공개됐다**(RapidAPI 실키·암호화 기본 키
  2건 실측, 2026-07-23). ⛔ **비공개 전환이 그 키들을 무효화하지 않는다** — 공개
  기간에 이미 수집됐다면 그대로 유효하므로, 실효 있는 조치는 **재발급**뿐이다.
  ✅ **2건 중 암호화 기본 키는 이미 끝났다** — 2026-08-12 셀프호스팅 컷오버 때
  `ENCRYPTION_KEY` 가 새 값으로 바뀌었고(그때 기존 행이 안 열려 오너가 08-13 에
  정리한 그 사고가 교체의 흔적이다), 소스의 폴백도 제거돼 지금은 없다
  (`scripts/check-env.ts` 가 "no fallback key exists" 라고 명시). 확인 방법은
  `~/selfhost/logs/cron.log` 의 최신 `encryption-key-audit` 줄 — `unreadable: 0`
  이면 두 복호화 경로 모두 정상이다. ✅ **RapidAPI 키도 끝났다(T-073 종결,
  2026-08-28)** — 이력에 박혔던 키는 오너가 이미 폐기한 3개 중 하나였고, 현행 키
  3개는 개발 `.env` 와 프로덕션 `~/selfhost/wagcrm/infra/selfhost/.env` 가 동일하며
  이력 노출이 없다(지문 대조 실측). ⇒ **지금 재발급할 시크릿은 없다.**
  🪤 **판정 범위를 좁게 잡지 말 것** — 2026-08-28 실사고: 한 세션이 개발 `.env`
  하나만 보고 "노출 키는 안 쓰는 키"라 단정했다가, 오너의 "다른 서비스에서도
  쓴다"는 진술과 접두가 겹쳐 뒤집혔고, 그 목록이 폐기분임이 확인돼 또 뒤집혔다.
  키 대조는 **등록처를 전부 열거한 뒤**(개발·프로덕션·CI·레포 밖 사용처) 판정한다.
  같은 세션이 이 문단을 "재발급이 통째로 남았다"로 읽고 그 부담을 근거로 새 레포
  컷을 권고한 일도 있었다 — **여러 건을 묶어 읽지 말 것.**
  소스에 폴백 리터럴로 키를 두지 말 것 —
  `hardcoded-secret-literals.contract.test.ts`가 강제한다. 기술 변경만 익명으로
  기술하고, 실측치는 대화 보고에만 담는다. 이미 푸시된 경우 정정은
  `--force-with-lease`로 한다.

- **PR Body Minimization (PR 본문 최소화, 2026-08-28 오너 확정):** PR 본문은
  템플릿의 표준 2줄(로컬 상세 포인터 + 검증 마커)로 끝낸다 — 무엇이 바뀌었는지는
  **제목**이 말한다. 실측 근거: 529건 동안 오너 직독 0건, 기계 소비는
  release-preflight 의 마커 grep 한 줄뿐, "PR 본문을 읽어라"는 추적 문서 참조 0건 —
  상세 본문은 소비자 없이 노출만 만든다. GitHub(커밋 메시지·PR 본문·리뷰 코멘트)에
  쓰지 않는 것(종류 기준): ①프로덕션 실측치 ②실명 ③**오너 확정 문구와 그 배경**
  (운영 규칙의 "왜") ④**알려진 한계·과거 실패 이력·미해결 리스크** ⑤검증 상세
  로그(픽셀·색상값·관찰 기록). 파일 경로·함수명·테스트 통과 수는 허용 — 코드에
  이미 있어 노출을 늘리지 않는다. 상세의 서식지는 로컬 핸드오프
  (`docs/handoff/pr-<NN>-<슬러그>.md`)와 로컬 서고(`docs/private/`)다. 검증 마커
  줄(`- [x] 배포 지침 및 검증 로그 확인 완료`)은 CI 가 grep 하므로 문자 그대로
  유지한다.
  🪤 **로컬 파일시스템이 없는 클라우드 세션의 출구** — 금지 범주 ④(알려진 한계·
  리스크)는 인계에 꼭 필요한 내용과 겹친다. 그 세션은 **PR 본문에 적지 말고
  소스 주석·계약 테스트에 남긴다**(문서 관리 정책이 원래 요구하는 영속 환원
  경로와 같다). PR 본문에는 「다음 세션이 볼 것: `<파일:줄>`」 포인터만 둔다 —
  적을 곳이 없어서 금지를 어기거나 리스크를 통째로 유실하는 제3의 선택지는 없다.

- **Seller-Facing Data Exposure:** 셀러가 보는 표면(포털 `/<slug>` ·
  `/p/[token]` · 성과 카드)은 해당 셀러 본인 데이터만 노출한다 — 타 셀러
  정보, 내부 원가·마진, 셀러 간 순위·집계 벤치마크는 어떤 형태로도 노출하지
  않는다(오너 확정). 포털 페이로드는 화이트리스트 방식
  (`src/lib/seller-portal.ts`)을 유지한다.

- **No Hallucinated Verification (상태 지레짐작 엄격 금지):** 원칙 정본은 전역
  `CORE.md` P0 **Proof of Work & Absolute Verification**이다 — 도구 호출 사실만으로
  성공을 보고하지 말고 결과를 독립 검증하라는 규칙. **이 레포에서 그 검증에 쓰는
  도구**는 `gh api`(PR·체크·커밋 상태) · `scripts/await-promotion.sh --check`(코드
  통합·prod 반영 2단계 판정)이며, 판정 계약은 P6(`docs/agents/deployment.md`)의
  Deployment Verification 절이다.
  <!-- contract:verify-deployment-status=retired section=agents-no-hallucinated-verification -->
  ⛔ **`scripts/verify-deployment.sh` 는 은퇴했다** — 종전 이 줄이 그것을 검증 도구로
  나열했지만, 그 스크립트는 **SHA 를 참조하지 않고** "가장 최근 Production 배포" 하나만
  집어 이 조항이 막으려는 성공 오보고를 **스스로 만들었다**(P6 🪤). 파일은 대체 경로를
  알려주는 묘비로 남아 항상 실패한다. 은퇴 사실은 여기와 P6 두 곳이 위와 같은 기계
  마커로 신고하며 `verify-deployment-doc-parity.contract.test.ts` 가 스크립트 실상태와
  대조한다 — **한 곳만 갱신하면 테스트가 깨진다.**

## Mandatory Reading Router (필독 라우터)

아래 표의 조건에 해당하는 작업은 **코드를 수정하기 전에** 지정 모듈을
**전문(全文)** 읽는다. 세션당 1회 읽고 캐시한다(같은 세션 내 재읽기 불요).
이것은 권고가 아니라 강제 규칙이다 — 모듈을 읽지 않고 해당 영역을 수정한
작업은 검증 미완으로 간주한다. 모듈에는 실사고에서 나온 금지·계약이 있다.
"일반 상식으로 아는 내용"이라는 이유로 건너뛰지 않는다.

| 작업 조건 | 필독 모듈 |
| --- | --- |
| 세션 첫 코드 수정 전(공통) | `docs/agents/codebase-map.md` — 스택·경로·코드 SSOT 표·문서 신뢰 지도 |
| 화면·UI·기능 변경(데스크톱/모바일 불문) | `docs/agents/product-ux.md` (P2·P3·P5) **+** `docs/agents/design-system.md` (P8) |
| 색·토큰·타이포·모션·애니메이션 | `docs/agents/design-system.md` (P8) |
| 커밋·푸시·PR·배포·마이그레이션·크론·시크릿 | `docs/agents/deployment.md` (P6) |
| 주문·정산·지표·집계·네이버/인스타 API·수집기·캐시 | `docs/agents/data-contracts.md` (P7) |
| dev 서버 기동·QA·테스트 작성/수정 | `docs/agents/dev-qa.md` (P9) |
| **지침·룰 `.md` 수정**(이 파일·`docs/agents/*`·전역 `~/.gemini/config/**`) | `~/.gemini/config/rules/rules-planning.md` §자기적용 — **승인 게이트** |

⚠️ **마지막 행은 2026-08-01 실사고에서 나왔다.** 그 게이트("금지/허용의 신설·해제,
스코프 확대는 **제안 → 승인 → 집행**")는 원래도 있었지만, 전역 Conditional Rule
Loading 이 그 파일을 *"계획 수립·작업 분해·설계 문서 작성"* 조건에만 걸어 둬서
**룰을 고치는 작업에서는 로드되지 않았다.** 사용자의 작업 지시("머지하게 해줘")를
룰 개정으로 번역하는 순간 범주가 바뀌는데 그 전환을 잡는 장치가 없었고, 실제로
게이트를 건너뛴 개정이 커밋 직전까지 갔다. 이 파일은 항상 로드되므로 전역 트리거를
못 읽은 세션도 여기서 걸린다.

## P1. Autonomy and Source-of-Truth Rules

> **정본은 전역 `~/.gemini/config/harness/CORE.md` P1이다** — 자율 실행
> (Full Autonomy Within P0)·확인 프롬프트 금지·비대화형 기본값·"프로젝트 문서가
> 학습 데이터를 이긴다"는 **모든 프로젝트 공통 작업 지침**이라 2026-08-01에 전역으로
> 승격했다. 여기엔 **이 레포의 예외와 좌표만** 남긴다.

- **이 레포의 자율 예외 — `scripts/*`와 `npm run dev`는 자율 범위 밖이다:**
  레포 `.env`의 `DATABASE_URL`이 **프로덕션 DB**라(위 P0 — 컷오버 후엔 셀프호스트
  스택이고 클라우드 Supabase 가 아니다) 스크립트 실행이 곧
  프로덕션 데이터 작업이다. 전역 P1의 "묻지 말고 실행"은 **읽기·로컬 작업에만**
  적용하고, 대량 수정·삭제 스크립트와 외부 부수효과(발주 메일·발송지연 API)는
  오너 확인을 받는다.

- **이 레포의 서브에이전트 좌표 — 정본은 전역 `CORE.md` P1.6 「Injected
  Self-Restraint vs. Routing Rule 5」와 `routing.md` 라우팅 규칙 5·8 이다:**
  Opus 5 세션에 기본 주입되는 `Do not call the AgentTool unless the user
  requested it` 은 **아래 완료의 정의 9번과 P8 라우팅 3번을 면제하지 않는다.**
  주입 출처·게이트 구조·허용(A 내부 / B 외부 레인)·금지 목록은 전역 정본에 있고
  **여기 복제하지 않는다.** 이 레포에서 A 군의 「프로젝트가 지정한 리뷰어」는
  `ss-ux-designer`(P8)다.

- **이 레포의 문서 우선 좌표:** Next 16.2는 대부분 모델의 학습 컷오프보다 최신이다 —
  App Router·캐시 API가 기억과 다르면 `node_modules/next/dist/docs/`(실존 확인됨)를
  정본으로 한다.

- **Existing Context Preservation:** 전역 지침은 기본값이며, 이 파일의 프로젝트별
  지침은 WAG CRM 작업 안에서 더 구체적인 기준이다(충돌 시 이 파일이 이긴다).

## P4. Execution Efficiency and Timeout Prevention

> **편집·복구·보고의 일반 규율 정본은 전역이다(2026-08-01 승격)** — 정밀 치환
> (퍼지 매칭 오치환 방지·스크립트 치환)·포트 복구는
> `~/.gemini/config/rules/rules-coding.md`(Precision Replacement · Dev Server
> Port Recovery), 컨텍스트 한계 시 핸드오프 기록 후 새 세션은
> `rules-planning.md`(Context Freeze Recovery), 성공 로그 덤프 금지·실패 시 첫
> 실행 가능 오류와 최소 근거만 보고·재현은 정확한 명령으로 유지는 `CORE.md` P4다.
> 여기엔 **이 레포의 구체 적용 목록만** 남긴다.

- **Concise Verification Reporting (전역 P4의 이 레포 적용 목록):** 루틴 검증
  명령 — `npm run build`, `npm test`, `npm run lint`, `npx tsc --noEmit`,
  `npm run release:check`, `npm run qa:smoke`, Git 커밋 훅 — 의 성공 출력은
  덤프하지 않는다. pass/fail·의미 있는 수치·**기존 경고**·미해결 운영 리스크만
  보고한다(빌드 로그·린트 결과표·테스트 진행 출력·패키지 매니저 출력·훅 출력
  포함).

## Documentation Management Policy (문서 관리 정책)

- **설계서·계획서는 로컬 서고 `docs/private/`에 쓴다(모드 L, 2026-08-28):**
  `docs/private/specs/` · `docs/private/plans/` 는 git 미추적이다. superpowers
  브레인스토밍/플랜 스킬의 기본 경로(`docs/superpowers/…`)는 이 프로젝트에서
  **이 경로로 오버라이드**된다 — 종전 `docs/superpowers/` 추적분 56건은
  2026-08-28 이곳으로 이관됐다(공개 전환 준비 설계,
  `docs/private/specs/2026-08-28-repo-republicize-prep.md`). 설계 근거·오너 확정
  배경·약점 목록이 담기므로 추적 승격 금지(계약:
  `archive-handoff-ignored.contract.test.ts`). 워크트리 세션도 메인 레포 사본에
  쓴다. 소스 주석의 「설계 정본:」 포인터는 이 로컬 경로를 가리켜도 된다 —
  경로 문자열 자체는 노출이 아니다.

- **1문서 1역할:** 새 영속 `.md`를 만들기 전에 `docs/agents/codebase-map.md`의
  문서 지도에서 같은 역할의 문서가 있는지 확인한다. 역할이 겹치면 신설 금지 —
  기존 문서를 갱신한다.
- **지도 등록 의무:** 새 영속 문서를 만들면 같은 PR에서 문서 지도에
  등록한다(역할·신뢰 등급). 지도에 없는 문서는 다음 정리 때 아카이브 대상으로
  간주한다.
- **보드 아키텍처(작업방식 우선, 2026-07-12 오너 지시):** 다세션 병렬 작업과
  머지 대기 누적은 이 프로젝트의 정상 상태다 — 보드를 줄수 목표로 조이지 않고
  구조로 흡수한다.
  - `PROJECT_MASTER.md` = **인덱스 보드**: 활성 항목당 **정확히 1줄**
    (상태 · 제목+PR# · 다음 게이트(누가 무엇을) · `docs/handoff/<slug>.md`
    링크). 서사·검증 로그·함정을 보드에 쓰지 않는다.
  - 항목 상세 = `docs/handoff/<slug>.md`(항목당 1파일, 템플릿
    `docs/handoff/README.md`). 소유권은 그 항목을 만든 세션에 있다 — 타 세션
    파일은 상태 줄 갱신 외 수정 금지.
  - 수명주기: 착지+검증 완료 → `PROJECT_LOG.md`에 요약 1줄 → 핸드오프 파일을
    `docs/archive/handoff/`로 이동 → 보드에서 줄 제거.
  - **마커 드리프트 점검 — `npm run board:check`(읽기 전용):** 보드는 손으로 쓰는
    주장 레지스트리라 "머지·배포됐는데 보드는 아직 대기"로 벌어진다. 이 스크립트가
    보드의 상태 문구를 gh(PR 상태)·git(`origin/main` 조상 여부 = 머지)·셀프호스트
    배포 마커(`~/selfhost/logs/deployed.sha` 조상 여부 = prod 반영)와 대조해 낡은
    대기 마커와 과대보고(배포 완료라는데 서버에 없음)를 표면화한다(종료코드
    0=일치·3=드리프트). ⛔ 종전 기준 `origin/release` 는 **SUPERSEDED**(2026-08-15)
    — 컷오버로 그 브랜치가 멈춰 컷오버 이후 머지분이 전부 위양성으로 쌓였다.
    마커가 없는 환경(클라우드 세션·fresh clone)은 **배포 판정 불가**로 명시하고
    미배포로 단정하지 않으며, 그 상태는 종료코드에 반영하지 않는다.
    **보드를 자동 수정하지 않는다** — 여러 세션이
    동시에 쓰는 git 미추적 파일이라 기계가 남의 줄을 덮어쓰면 복구 수단이 없다.
    세션 시작 시 또는 정리 패스 전에 돌리고, 수정은 항목 소유 세션이 상태 줄만
    갱신한다. 배경: 2026-07-21 전수 정정 패스 후 **8일 만에 재발**해 07-29 에는
    보드 참조 PR 42건이 전부 MERGED 인데 대기 마커가 16건 남아 있었다(완료된 작업에
    재착수 지시가 나가는 사고). 판정 계약은
    `scripts/__tests__/board-drift-check.test.ts`가 고정한다.
  - **⛔ 보드는 앵커 문자열로만 수정한다 — 행 번호·배열 인덱스 쓰기 금지.**
    여러 세션이 동시에 줄을 삽입하므로 **인덱스는 읽은 순간 이미 낡는다.**
    실사고(2026-07-30): 한 세션이 `lines[1] = 새줄`로 자기 줄을 고치려다 그 사이
    삽입된 **타 세션 줄을 덮어썼고**, git 미추적이라 무엇이 지워졌는지조차 확정하지
    못했다. ①앵커로 찾고 **미발견이면 중단** ②치환은 1회만(전역 치환 금지)
    ③줄 제거는 **제거 개수 검증** ④백업은 **레포 밖**(scratchpad)에 두고 **같은
    턴에 지우지 않는다**(지우면 사고 시 비교 대상이 없다 — 모드 L 파일의 레포 내
    `.bak`은 그 자체가 P0였다) ⑤편집 후 `diff`로 **타 세션 줄 무손상**을 확인한다.
    ⚠️ `board:check`는 **탐지**만 한다 — 이 사고는 **쓰기** 단계에서 났다.
  - **줄을 지우는 정리 패스는 "제거분이 `PROJECT_LOG` 에 있는가"를 기계로 대조한다.**
    수명주기대로 이관→제거를 하더라도, 스윕 로직이 **완료계가 아닌 줄을 함께 집는
    사고**가 난다(2026-07-31 실발생 — 대기 상태 항목 1건이 딸려 나간 것을 이 대조가
    잡아 백업에서 원위치 복구했다). 제거 후 `before` 대비 사라진 줄을 전부 `PROJECT_LOG`
    에서 찾아 **미보존 0건**을 확인하고, 진행·대기 마커 항목이 사라지지 않았는지도
    따로 센다. 대조 없이 "옮겼으니 괜찮다"고 넘기지 말 것.
  - **좌표 없는 항목을 만들지 않는다.** 항목이 PR 번호도
    `docs/handoff/<slug>.md` 링크도 없으면, 보드 줄이 유실될 때 **어디에도 흔적이
    남지 않는다**(gh·`PROJECT_LOG` 어느 쪽으로도 재구성 불가). 위 사고 조사에서
    PR 있는 항목은 전부 대조로 확인됐지만 그 부류만 판정 불가로 남았다.
    `npm run board:check`가 이 항목들을 **경고로** 표면화한다(종료코드에 반영하지
    않는다 — 도입 시점 백로그로 즉시 실패가 뜨면 점검기가 무시당한다).
    ⚠️ 두 사본 모두 여전히 git 미추적이다 — 이 규약은 **한 줄 유실**에 견디게
    하는 것이고 파일 전체 유실은 막지 못한다(별개 문제).
  - 기존 보드의 서사형 항목은 소급 재작성하지 않는다(타 세션 항목 보존) — 각
    항목이 착지할 때 자연 이관한다.
  - **로컬 전용(모드 L, 2026-07-12 시행):** `PROJECT_MASTER.md` ·
    `PROJECT_LOG.md` · `docs/handoff/*`(README 제외) ·
    **`docs/archive/handoff/*`**(위 수명주기의 이관처 — 원본과 같은 등급이어야
    한다. 2026-07-30 이전에는 이 경로가 무시되지 않아, 정책대로 옮기는 행위가
    로컬 전용 파일을 public 레포의 추적 후보로 승격시켰다. 계약 테스트
    `archive-handoff-ignored.contract.test.ts`가 짝을 고정한다)는 git 미추적이다 —
    실명·실측치를 자유롭게 기록한다(P0의 공개 레포 금지는 추적 파일에만
    적용). 워크트리 세션도 자기 사본이 아니라 **메인 레포 루트의 단일 사본**
    (`git worktree list` 첫 항목 경로)을 직접 읽고 쓴다 — PR에 보드 변경을
    싣지 않는다. ⚠️ **역방향 사고 주의** — 그 `cd <메인레포>` 습관 그대로
    **추적 파일**을 메인 레포에서 편집·커밋하면 그때 그 레포가 올라가 있던
    **타 세션 브랜치로 커밋이 들어간다**(2026-07-31 실발생 — 내 PR 은 빈 채로
    올라갔다). 메인 레포 `cd` 는 모드 L 파일 편집에만 쓰고, 추적 파일은 자기
    워크트리에서 만진다. 탐지·복구는 `docs/agents/deployment.md` Shared Tree
    Hygiene. 이 파일들은 git 백업이 없으므로 영속 가치가 있는 결정은
    착지 시 코드·계약 테스트·`docs/agents/` 모듈로 환원한다.
- **계획서·산출물 이관:** 계획서(`*_PLAN.md`)는 완료 내용이 본문을 압도하기
  시작하면 완료분을 `PROJECT_LOG.md`로 이관한다. 일회성 워크시트·디버그
  덤프·검증 캡처 문서는 해당 작업 종료 시 `docs/archive/`로 이동한다.
- **구현 현황의 정본은 `FEATURE_SPEC.md` 하나다.** 상태 요약 문서를 추가로
  만들지 않는다.

## Definition of Done — 작업 종료 전 순서대로 확인한다

1. `npm run typecheck`(= `tsc --noEmit`)과 `npm run lint`(또는 변경 파일
   eslint) 클린 — 기존 경고는 보고에 명시. ⚠️ **맨 `npx tsc --noEmit` 을 쓰지
   말 것** — sqlite Prisma Client 생성물은 미추적이라(`.gitignore`) fresh
   clone·신규 워크트리에서는 `src/lib/prisma-client.ts` 의 정적 import 가
   `TS2307` 로 넘어진다. `npm run typecheck` 은 `pretypecheck` 훅으로
   `scripts/ensure-sqlite-client.mjs` 를 먼저 태운다(웜 ~90ms).
2. `npm test` 신규 회귀 0 — 선재 실패는 origin/main 재현으로 분리 입증.
3. UI 변경이면 실렌더 확인(dev 서버/스크린샷), 모바일 표면이면 UA 분기
   확인, 애니메이션이면 `review-animations` 통과(P8).
4. 배포로 이어지는 변경이면 `npm run release:check`(main push 시
   `release-preflight.yml`이 동일 번들을 재검증한다).
5. 스키마 변경이면 `prisma migrate dev`로 마이그레이션 생성(배포 시 자동
   적용) — `guard` 필수 체크가 shadow DB로 스키마↔마이그레이션 동기화 검증(P6).
6. 커밋·PR에 셀러 실명·실측치·시크릿 없음(P0), 제목 규약 준수 —
   `type(scope): 한글 요약` + PR 생성 직후 `#NN ` 프리픽스(P6).
   **CLI로 PR 생성 시 템플릿 덮어쓰기로 인한 `Release Preflight` 실패를 방지하기 위해, PR 본문에 반드시 `- [x] 배포 지침 및 검증 로그 확인 완료` 마커를 포함한다.**
   본문은 P0 「PR 본문 최소화」의 표준 2줄로 끝낸다 — 상세는 로컬 핸드오프 파일에.
7. `PROJECT_MASTER.md` Handoff 갱신 — 완료·잔여·다음 에이전트 지시(필독
   경로 분류 포함). 워크트리 세션은 메인 레포 사본에 쓴다. 로컬 파일시스템이
   없는 환경(클라우드 세션)만 PR 본문으로 대체하되 P0 최소화 금지 범주를 지킨다.
8. 보고는 성공 로그 덤프 없이 pass/fail과 의미 있는 수치만(P4).
9. **전문가 에이전트 교차 검증 (Subagent Cross-Validation):** 다음 3가지 조건 중 하나라도 만족하면 작업 종료 전 반드시 `Agent` 도구로 `code-reviewer` 등 전문가 서브 에이전트를 소환해 (1)PR 룰 준수 및 (2)실제 로그 검증 여부를 교차 검증받는다. (조건: 순수 코드 변경량 100줄 이상(자동 생성 파일 제외), `prisma/schema.prisma` 변경, 인증/권한 핵심 로직 변경) ⚠️ 세션에 기본 주입되는 `Do not call the AgentTool unless the user requested it` 은 **이 항목을 면제하지 않는다** — 위 P1 좌표가 가리키는 전역 `CORE.md` P1.6 조문이 그 「사용자 요청」에 해당한다. 종전 문언이 지목한 `invoke_subagent` 는 **이 하네스에 존재하지 않는 도구명**이었다(2026-08-26 정정).
