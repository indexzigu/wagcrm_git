import { PortalNotFound } from "@/components/portal/portal-status-screen";

// 셀러 포털 구간의 notFound() 화면 — Next 기본 영어 404 대신 셀러용 안내를 준다.
export default function NotFound() {
  return <PortalNotFound />;
}
